#!/bin/ksh


arguments=$*

eval `cclargs_lite $0 \
  -c        ""                    ""                           "[cfg to run         ]" \
  -o        ""                    ""                           "[output results     ]" \
  -i        ""                    ""                           "[input config to eval     ]" \
  -h        ""                    ""                           "[Display help       ]" \
  ++ $arguments`                                                                                                                                                                                                                   

. $c 

unset SEQ_GETDEF_DISABLED
while read LINE ; do eval echo $(echo $LINE | sed 's/;/\n/g') ; done < $i > $o  

